# scraper

Fetch, store, and scrape web pages (Go).

Dual-licensed under MIT or the UNLICENSE.

```bash
go install github.com/arclabs561/scraper@latest

# Example (see `scraper.go` / `cmd/` for flags):
scraper fetch https://example.com
```
